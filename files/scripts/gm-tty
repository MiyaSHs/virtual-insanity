#!/usr/bin/env bash
set -euo pipefail
if [[ "${EUID:-$(id -u)}" -ne 0 ]]; then
  echo "Run as root: sudo gm-tty" >&2
  exit 1
fi
systemctl stop sddm || true
systemctl set-default multi-user.target || true
echo "Switched back to TTY. Log in on tty1."
