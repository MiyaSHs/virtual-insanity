#!/usr/bin/env bash
set -euo pipefail
REQ="/run/gm/next-session"
if [[ ! -e "$REQ" ]]; then
  echo "Session switch request file not found ($REQ). Is gm-session-switchd enabled?" >&2
  exit 1
fi
echo "gamemode" > "$REQ"
echo "Requested Game Mode session. You will be logged out and auto-switched."
